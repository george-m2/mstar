Source code and pre-trained models for evaluating adversarial robustness in Synthetic Aperture Radar (SAR) Automatic Target Recognition (ATR) systems.

.pth model checkpoints can be cloned using Git LFS.