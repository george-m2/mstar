# Non-uv fallback for cluster environments that prefer pip.
# For reproducible installs we recommend `uv sync` against pyproject.toml + uv.lock.
# Torch versions pinned loosely; the cluster's CUDA build of torch should be
# installed first via the appropriate index (see slurm/env_setup.sh).

torch>=2.0
torchvision>=0.15
torchattacks>=3.5.1
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
tqdm>=4.65
pyyaml>=6.0
kagglehub>=0.3
